import { Text, View, SafeAreaView } from "react-native";
import React, { Component } from "react";

export default function RegisterSex({ navigation }) {
  return (
    <SafeAreaView>
      <View>
        <Text>RegisterSex</Text>
      </View>
    </SafeAreaView>
  );
}
